<div class="row">

    <h1>Actions</h1>

    <table name="users">
        <tr>
            <th>Names</th>
            <th>Count</th>
        </tr>
        {{#each actions}}
        <tr>
            <td><a href="/counter/{{greet_name}}">{{greet_name}}</a></td>
            <td>{{greet_count}}</td>

        </tr>
        {{/each}}
    </table>
</div>